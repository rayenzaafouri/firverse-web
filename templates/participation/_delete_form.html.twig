<form method="post" action="{{ path('app_participation_delete', {'participationID': participation.participationID}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participation.participationID) }}">
    <button class="btn">Delete</button>
</form>
