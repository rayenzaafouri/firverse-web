{% extends 'base.html.twig' %}

{% block title %}Admin - Manage Discounts{% endblock %}
<<<<<<< HEAD
{% block stylesheets %}
<style>

</style>
{% endblock %}
{% block body %}
{% include 'navbar.html.twig' %}

<div class="container py-5">
    <h1 class="mb-4">Manage Discounts</h1>

    <div class="mb-3">
        <a href="{{ path('app_product_discount_new') }}" class="btn btn-success">+ Create New Discount</a>
    </div>

    <div class="table-responsive">
        <table class="table table-striped text-center">
            <thead class="thead-dark">
                <tr>
                    <th>Actions</th>
                    <th>ID</th>
                    <th>Product</th>
                    <th>Discount %</th>
                    <th>Valid From</th>
                    <th>Valid Until</th>
                </tr>
            </thead>
            <tbody>
            {% for product_discounts in product_discounts %}
                <tr>
                    <td class="d-flex justify-content-center gap-2">
                        <a href="{{ path('app_product_discount_show', {'id': product_discounts.id}) }}" class="btn btn-sm btn-primary mr-1">View</a>
                        <a href="{{ path('app_product_discount_edit', {'id': product_discounts.id}) }}" class="btn btn-sm btn-warning mr-1">Edit</a>
                        {{ include('back/shop/product_discount/_delete_form.html.twig', { product_discounts: product_discounts }) }}
                    </td>
                    <td>{{ product_discounts.id }}</td>
                    <td>{{ product_discounts.product.name }}</td>
                    <td>{{ product_discounts.discountPercentage }}%</td>
                    <td>{{ product_discounts.validFrom ? product_discounts.validFrom|date('Y-m-d') : '' }}</td>
                    <td>{{ product_discounts.validUntil ? product_discounts.validUntil|date('Y-m-d') : '' }}</td>
                    
                </tr>
            {% else %}
                <tr>
                    <td colspan="6" class="text-muted">No discounts found.</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
=======

{% block stylesheets %}
{{ parent() }}
<link rel="stylesheet" href="{{ asset('/styles/admin.css') }}">
{% endblock %}

{% block body %}
<div class="admin-dashboard-layout">
    <div id="sidenavAccordion">
        {% include 'back/admin/sidenav.html.twig' %}
    </div>

    <div class="container py-5">
        <h1 class="mb-4">Manage Discounts</h1>

        <div class="mb-3">
            <a href="{{ path('app_product_discount_new') }}" class="btn btn-success">+ Create New Discount</a>
        </div>

        <div class="table-responsive">
            <table class="table table-striped text-center">
                <thead class="thead-dark">
                    <tr>
                        <th>Actions</th>
                        <th>ID</th>
                        <th>Product</th>
                        <th>Discount %</th>
                        <th>Valid From</th>
                        <th>Valid Until</th>
                    </tr>
                </thead>
                <tbody>
                {% for product_discounts in product_discounts %}
                    <tr>
                        <td class="d-flex justify-content-center gap-2">
                            <a href="{{ path('app_product_discount_show', {'id': product_discounts.id}) }}" class="btn btn-sm btn-primary">View</a>
                            <a href="{{ path('app_product_discount_edit', {'id': product_discounts.id}) }}" class="btn btn-sm btn-warning">Edit</a>
                            {{ include('back/shop/product_discount/_delete_form.html.twig', { product_discounts: product_discounts }) }}
                        </td>
                        <td>{{ product_discounts.id }}</td>
                        <td>{{ product_discounts.product.name }}</td>
                        <td>{{ product_discounts.discountPercentage }}%</td>
                        <td>{{ product_discounts.validFrom ? product_discounts.validFrom|date('Y-m-d') : '' }}</td>
                        <td>{{ product_discounts.validUntil ? product_discounts.validUntil|date('Y-m-d') : '' }}</td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="6" class="text-muted">No discounts found.</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
>>>>>>> shop
    </div>
</div>
{% endblock %}
