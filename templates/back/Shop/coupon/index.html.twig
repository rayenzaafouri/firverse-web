{% extends 'base.html.twig' %}

{% block title %}Manage Coupons{% endblock %}
<<<<<<< HEAD
{% block stylesheets %}
<style>

</style>
{% endblock %}
{% block body %}
{% include 'navbar.html.twig' %}

<div class="container py-5">
    <h1 class="mb-4">Manage Coupons</h1>

    <a href="{{ path('app_coupon_new') }}" class="btn btn-success mb-3">+ Create New Coupon</a>

    <table class="table table-striped text-center align-middle">
          <thead class="bg-dark">
            <tr>
                <th>Actions</th>
                <th>Code</th>
                <th>Discount %</th>
                <th>Valid From</th>
                <th>Valid Until</th>
                <th>Status</th>
            </tr>
        </thead>
        <tbody>
            {% for coupon in coupons %}
                <tr>
                <td class="d-flex justify-content-center gap-2">
                        <a href="{{ path('app_coupon_show', {'id': coupon.id}) }}" class="btn btn-sm btn-primary mr-1">View</a>
                        <a href="{{ path('app_coupon_edit', {'id': coupon.id}) }}" class="btn btn-sm btn-warning mr-1">Edit</a>
                        {{ include('back/shop/coupon/_delete_form.html.twig')}}
                    </td>
                    <td>{{ coupon.code }}</td>
                    <td>{{ coupon.discount_percentage }}</td>
                    <td>{{ coupon.valid_from|date('Y-m-d H:i') }}</td>
                    <td>{{ coupon.valid_until|date('Y-m-d H:i') }}</td>
                    <td>
                        {% if coupon.active %}
                            <span class="badge bg-primary">Active</span>
                        {% else %}
                            <span class="badge bg-danger">Inactive</span>
                        {% endif %}
                    </td>
                    
                </tr>
            {% else %}
                <tr>
                    <td colspan="6" class="text-muted text-center">No coupons found.</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
=======

{% block stylesheets %}
{{ parent() }}
<link rel="stylesheet" href="{{ asset('/styles/admin.css') }}">
{% endblock %}

{% block body %}
<div class="admin-dashboard-layout">
    <div id="sidenavAccordion">
        {% include 'back/admin/sidenav.html.twig' %}
    </div>

    <div class="container py-5">
        <h1 class="mb-4">Manage Coupons</h1>

        <a href="{{ path('app_coupon_new') }}" class="btn btn-success mb-3">+ Create New Coupon</a>

        <table class="table table-striped text-center align-middle">
            <thead class="bg-dark text-white">
                <tr>
                    <th>Actions</th>
                    <th>Code</th>
                    <th>Discount %</th>
                    <th>Valid From</th>
                    <th>Valid Until</th>
                    <th>Status</th>
                </tr>
            </thead>
            <tbody>
                {% for coupon in coupons %}
                    <tr>
                        <td class="d-flex justify-content-center gap-2">
                            <a href="{{ path('app_coupon_show', {'id': coupon.id}) }}" class="btn btn-sm btn-primary mr-1">View</a>
                            <a href="{{ path('app_coupon_edit', {'id': coupon.id}) }}" class="btn btn-sm btn-warning mr-1">Edit</a>
                            {{ include('back/shop/coupon/_delete_form.html.twig') }}
                        </td>
                        <td>{{ coupon.code }}</td>
                        <td>{{ coupon.discount_percentage }}</td>
                        <td>{{ coupon.valid_from|date('Y-m-d H:i') }}</td>
                        <td>{{ coupon.valid_until|date('Y-m-d H:i') }}</td>
                        <td>
                            {% if coupon.is_active %}
                                <span class="badge bg-primary">Active</span>
                            {% else %}
                                <span class="badge bg-danger">Inactive</span>
                            {% endif %}
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="6" class="text-muted text-center">No coupons found.</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
>>>>>>> shop
</div>
{% endblock %}
