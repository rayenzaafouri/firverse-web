{% import 'macros/form_errors.html.twig' as formErrors %}
{% extends 'base.html.twig' %}

{% block title %}Edit Coupon{% endblock %}
<<<<<<< HEAD
{% block stylesheets %}
<style>

</style>
{% endblock %}
{% block body %}
{% include 'navbar.html.twig' %}

<div class="container py-5">
    <h1 class="mb-4 text-center">Edit Coupon</h1>

    <div class="card shadow p-4">
        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

        <div class="form-group">
            {{ form_label(form.code) }}
            {{ form_widget(form.code, {'attr': {'class': 'form-control'}}) }}
            <div class="text-danger">
                {{ form_errors(form.code) }}</div>
        </div>

        <div class="form-group">
            {{ form_label(form.discount_percentage) }}
            {{ form_widget(form.discount_percentage, {'attr': {'class': 'form-control'}}) }}
            <div class="text-danger">
                {{ form_errors(form.discount_percentage) }}</div>
        </div>

        <div class="form-group">
            {{ form_label(form.valid_from) }}
            {{ form_widget(form.valid_from, {'attr': {'class': 'form-control'}}) }}
            <div class="text-danger">
                {{ form_errors(form.valid_from) }}</div>
        </div>

        <div class="form-group">
            {{ form_label(form.valid_until) }}
            {{ form_widget(form.valid_until, {'attr': {'class': 'form-control'}}) }}
            <div class="text-danger">
                {{ form_errors(form.valid_until) }}</div>
        </div>

        <div class="form-group">
            {{ form_label(form.is_active) }}
            {{ form_widget(form.is_active, {'attr': {'class': 'form-check-input ml-2'}}) }}
            <div class="text-danger">
                {{ form_errors(form.is_active) }}</div>
        </div>

        <div class="form-group">
            {{ form_label(form.min_order_amount) }}
            {{ form_widget(form.min_order_amount, {'attr': {'class': 'form-control'}}) }}
            <div class="text-danger">
                {{ form_errors(form.min_order_amount) }}</div>
        </div>
        
        <div class="text-center">
            <button class="btn btn-warning mt-3">Update</button>
            <a href="{{ path('app_coupon_index') }}" class="btn btn-secondary mt-3 ml-2">Cancel</a>
        </div>

        {{ form_end(form) }}
=======

{% block stylesheets %}
{{ parent() }}
<link rel="stylesheet" href="{{ asset('/styles/admin.css') }}">
{% endblock %}

{% block body %}
<div class="admin-dashboard-layout">
    <div id="sidenavAccordion">
        {% include 'back/admin/sidenav.html.twig' %}
    </div>

    <div class="container py-5">
        <h1 class="mb-4 text-center">Edit Coupon</h1>

        <div class="card shadow p-4">
            {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

            <div class="form-group">
                {{ form_label(form.code) }}
                {{ form_widget(form.code, {'attr': {'class': 'form-control'}}) }}
                <div class="text-danger">{{ form_errors(form.code) }}</div>
            </div>

            <div class="form-group">
                {{ form_label(form.discount_percentage) }}
                {{ form_widget(form.discount_percentage, {'attr': {'class': 'form-control'}}) }}
                <div class="text-danger">{{ form_errors(form.discount_percentage) }}</div>
            </div>

            <div class="form-group">
                {{ form_label(form.valid_from) }}
                {{ form_widget(form.valid_from, {'attr': {'class': 'form-control'}}) }}
                <div class="text-danger">{{ form_errors(form.valid_from) }}</div>
            </div>

            <div class="form-group">
                {{ form_label(form.valid_until) }}
                {{ form_widget(form.valid_until, {'attr': {'class': 'form-control'}}) }}
                <div class="text-danger">{{ form_errors(form.valid_until) }}</div>
            </div>

            <div class="form-group">
                {{ form_label(form.is_active) }}
                {{ form_widget(form.is_active, {'attr': {'class': 'form-check-input ml-2'}}) }}
                <div class="text-danger">{{ form_errors(form.is_active) }}</div>
            </div>

            <div class="form-group">
                {{ form_label(form.min_order_amount) }}
                {{ form_widget(form.min_order_amount, {'attr': {'class': 'form-control'}}) }}
                <div class="text-danger">{{ form_errors(form.min_order_amount) }}</div>
            </div>

            <div class="text-center">
                <button class="btn btn-warning mt-3">Update</button>
                <a href="{{ path('app_coupon_index') }}" class="btn btn-secondary mt-3 ml-2">Cancel</a>
            </div>

            {{ form_end(form) }}
        </div>
>>>>>>> shop
    </div>
</div>
{% endblock %}
